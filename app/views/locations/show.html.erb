<%= render ModalComponent.new do %>
	<%= render FieldsComponent.new(fields: @fields) %>
	<%= render SubmitComponent.new(submit: (current_user.admin? or current_user.is_coach?) ? edit_location_path(@location) : nil) %>
<% end %>
