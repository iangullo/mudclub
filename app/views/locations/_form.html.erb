<%= form_for location, data: { title: title, season: season, turbo_frame: "_top" } do |form| %>
	<div id="locations-form">
		<table>
			<div id="locations-form-header">
				<tr>
		      <th rowspan="2">
		        <%= image_tag("location.svg", size: "50x50") %>
		      </th>
		      <th colspan="2" align="left" class="align-top rounded-md text-yellow-600">
		        <%= title %>
		      </th>
				</tr>
			</div>
			<div id="locations-form-data">
				<tr>
					<td align="left" colspan="2"><%= form.text_field :name, autofocus: true, autocomplete: t(:h_name), class: "rounded border shadow-inner" %></td>
				</tr><tr>
					<td align="right"><%= image_tag "gmaps.svg", size: "25x25", alt: "GPS"  %></td>
		      <td><%= form.text_field :gmaps_url, autocomplete: t(:l_loc_gmaps), class: "rounded border shadow-inner" %></td>
		    </tr><tr>
		      <td align="right"><%= image_tag "training.svg", size: "25x25", alt: t(:a_loc_train)  %>
		        <td align="left" class="inline-flex"><%= t(:l_loc_train) %><%= form.check_box :practice_court %></td>
		  	</tr>
			</div>
		</table>
		<%= form.hidden_field(:season_id, value: season.id) if season %>
		<%= render "shared/modal_bottom", save: true %>
	</div>
<% end %>
