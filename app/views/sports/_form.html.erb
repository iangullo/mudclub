<%= form_for @sport, authenticity_token: true, data: {turbo_frame: "_top"} do |form| %>
	<div id="edit_sport">
		<% @fields.form = form %>
		<%= render @fields %>
		<%= render @submit %>
	</div>
<% end %>
