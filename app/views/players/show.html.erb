<%= render "shared/modal", simple: true, edit: (current_user.admin? or current_user.is_coach? or current_user.person.player_id==@player.id) ? edit_player_path(@player) : nil do %>
	<%= render FieldsComponent.new(fields: @fields) %>
<% end %>
