<%= render ModalComponent.new do %>
	<%= render @title %>
	<div class="flex justify-center">
		<%= render @fields %>
		<% if @table %>
			<div class="flex justify-center">
				<%= render @table %>
			</div>
		<% end %>
	</div>
	<%= render @submit %>
<% end %>
