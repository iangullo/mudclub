<div id="member-show">
	<%= render @fields %>
	<% if @table %>
		<div class="flex justify-center">
			<%= render @table %>
		</div>
		<%= paginate @page if @page %>
	<% end %>
	<%= render @submit %>
</div>
