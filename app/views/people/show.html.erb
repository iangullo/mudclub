<%= render ModalComponent.new do %>
	<div class="content">
		<%= render @title %>
		<%= render @fields %>
		<% if @table %>
			<div class="flex justify-center">
				<%= render @table %>
			</div>
		<% end %>
		<%= render @submit %>
	</div>
<% end %>
