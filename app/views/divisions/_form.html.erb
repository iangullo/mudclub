<%= form_with model: division, authenticity_token: true, data: {turbo_frame: "_top"} do |form| %>
  <%= render HeaderComponent.new(header: @header, form: form) %>
  <%= render "shared/modal_bottom", save: true %>
<% end %>
