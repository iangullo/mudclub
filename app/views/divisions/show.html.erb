<%= render ModalComponent.new do %>
	<%= render FieldsComponent.new(fields: @fields) %>
	<%= render SubmitComponent.new(submit: current_user.admin? ? edit_division_path(@division) : nil) %>
<% end %>
