<%= render ModalComponent.new do %>
  <div id="team-roster">
    <%= render FieldsComponent.new(fields: @title) %>
    <%= line_chart @att_data, legend: false %>
    <%= render GridComponent.new(grid: @grid) %>
    <%= render SubmitComponent.new(submit: nil) %>
  </div>
<% end %>