<div id="team-show">
	<%= render @title %>
	<%= render @links if @links %>
	<%= render @coaches %>
	<%= render @grid  if @grid %>
	<%= render @calendar if @calendar %>
	<%= render @submit if @submit %>
</div>
