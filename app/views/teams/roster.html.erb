<%= render "shared/modal", simple: true, edit: (current_user.admin? or @team.has_coach(current_user.person.coach_id)) ? edit_roster_team_path : nil do %>
  <div id="team-roster">
    <%= render FieldsComponent.new(fields: @header) %>
    <%= render GridComponent.new(g_head: @grid[:header], g_rows: @grid[:rows]) %>
<% end %>
