<div id="team-roster">
  <%= render FieldsComponent.new(fields: @title) %>
  <%= render GridComponent.new(grid: @grid) %>
  <%= render SubmitComponent.new(close: "back", close_return: :back, submit:(current_user.admin? or @team.has_coach(current_user.person.coach_id)) ? edit_roster_team_path : nil) %>
</div>