<%= render ModalComponent.new do %>
	<%= form_for @user, authenticity_token: true, data: {controller: "mandatory-fields", turbo_frame: "_top"} do |form| %>
		<% @role.form     = form %>
		<%= form.fields_for :person do |f| %>
			<% @header.form   = f %>
			<% @p_fields.form = f %>
			<%= render @header %>
			<%= render @role %>
			<%= render @p_fields %>
			<% if @k_fields %>
				<% @k_fields.form = form %>
				<%= render @k_fields %>
			<% end %>
		<% end %>
		<%= render @submit %>
	<% end %>
<% end %>
