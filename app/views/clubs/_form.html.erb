<%= render ModalComponent.new do %>
	<%= form_for @club, authenticity_token: true, data: {controller: "mandatory-fields", turbo_frame: "_top"} do |form| %>
		<% @mtitle.form = form %>
		<% @fields.form = form %>
		<%= render @mtitle %>
		<%= render @fields %>
		<%= render @submit %>
	<% end %>
<% end %>