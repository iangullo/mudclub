<%= render ModalComponent.new do %>
	<%= form_with model: season, authenticity_token: true, data: {turbo_frame: "_top"} do |form| %>
		<%= render FieldsComponent.new(fields: @fields, form: form) %>
		<%= render SubmitComponent.new(close: "cancel", submit: "save") %>
	<% end %>
<% end %>
