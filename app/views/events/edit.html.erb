<% if @event.rest? %>
	<%= render ModalComponent.new do %>
		<%= render 'form', event: @event, title: @event.title %>
	<% end %>
<% else %>
	<%= render 'form', event: @event, title:  @event.train? ? @event.title : @event.team.to_s %>
<% end %>
